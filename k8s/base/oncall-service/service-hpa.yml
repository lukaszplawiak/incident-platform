apiVersion: v1
kind: Service
metadata:
  name: oncall-service
  labels:
    app: oncall-service
    app.kubernetes.io/part-of: incident-platform
spec:
  selector:
    app: oncall-service
  ports:
    - name: http
      port: 8086
      targetPort: 8086
    - name: management
      port: 8096
      targetPort: 8096
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: oncall-service-hpa
  labels:
    app.kubernetes.io/part-of: incident-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: oncall-service
  minReplicas: 1
  maxReplicas: 2
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80